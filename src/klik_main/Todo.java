package klik_main;

public class Todo {

	// mouse wheel browsing does not work for dirs with a few (eg 2) images
	
	// find a way that focus goes back to main display after a minipanel
	// i.e. move
	
	// when displaying in 4x4, if there is less than 16 images in the folder
	// one gets one the first image 16 times, or the last one, 16 times
}
